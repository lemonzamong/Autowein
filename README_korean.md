# Autowein: 모빌리티 인텔리전스를 위한 인지형 디지털 트윈 (Cognitive Digital Twin)

> **"스스로 토론하며 진실을 찾아내는 AI 분석가"**

**Autowein**은 글로벌 자동차 산업을 모니터링하고, 지속적인 지식 그래프(Knowledge Graph)를 유지하며, 다중 에이전트(Multi-Agent) 추론을 통해 전략적 통찰력을 생성하도록 설계된 고급 AI 인텔리전스 시스템입니다. 단순한 뉴스 스크래퍼와 달리, 최고 수준의 시장 분석가의 **인지형 디지털 트윈(Cognitive Digital Twin, CDT)** 역할을 수행하며, **역강화학습(IRL)**을 사용하여 노이즈를 필터링하고 **토론 프로토콜(Debate Protocols)**을 통해 자신의 예측을 검증합니다.

---

## 🏛️ 시스템 아키텍처

이 시스템은 4단계의 순차적 파이프라인으로 작동하며, CLI 또는 웹 대시보드(Web Dashboard)를 통해 제어됩니다.

```mermaid
graph TD
    subgraph 1단계: 게이트키퍼 (Gatekeeper)
    A[원시 뉴스 스트림] -->|스크래퍼| B(하이브리드 점수 엔진)
    B -->|SBERT + TF-IDF| C{임계값 확인}
    C -->|통과| D[다양성 클러스터링]
    D -->|그룹화| E[1_selected_ranked.json]
    end

    subgraph 2단계: 큐레이션 (Curation)
    E -->|Streamlit/웹 UI| F(인간 전문가 검토)
    F -->|상위 10개 선정| G[2_curated.json]
    end

    subgraph 3단계: 인지 분석 (Cognitive Analysis)
    G --> H(역사가 엔진)
    H <-->|2-Hop 쿼리| I[(Neo4j 지식 그래프)]
    H -->|컨텍스트 + 뉴스| J(분석가 엔진)
    
    subgraph 에이전트 토론 루프
    J --> K[계획자 (Planner)]
    K --> L[시뮬레이터 (Simulator)]
    L --> M[악마의 변호인 (Devil's Advocate)]
    M -->|비평| N[종합자 (Synthesizer)]
    N -->|수정된 계획| O[작가 (Writer)]
    end
    
    O --> P[3_analyzed.json]
    end

    subgraph 4단계: 내보내기 (Export)
    P -->|시각화 생성기| Q[Final Report.md]
    end
```

### 🔬 상세 파이프라인 워크플로우

각 단계에서 실행되는 구체적인 로직은 다음과 같습니다.

#### **1단계: 게이트키퍼 (Gatekeeper) - 노이즈 필터링**
**목표**: 매일 쏟아지는 1,000개 이상의 노이즈 뉴스 항목을 약 50개의 고가치 후보로 줄입니다.
1.  **수집 (Ingestion)**: 설정된 키워드(예: "EV", "전고체 배터리")에 대해 Google News RSS를 스크래핑합니다.
2.  **SBERT 임베딩**: 제목과 요약문을 `mpnet-base-v2`를 사용하여 768차원 밀집 벡터(dense vector)로 변환합니다.
3.  **하이브리드 점수 엔진**: 모든 기사에 대해 복합 점수를 계산합니다:
    *   **관련성 (30%)**: TF-IDF 키워드 매칭.
    *   **의미적 유사성 (40%)**: "이상적인 주제" 클러스터와의 코사인 유사도 (SBERT 사용).
    *   **선호도 (30%)**: 사용자의 과거 큐레이션 선택을 학습한 **IRL 보상 모델(Reward Model)** 점수.
4.  **다양성 클러스터링**: **레벤슈타인 거리(Levenshtein Distance)**(텍스트)와 **코사인 유사도(Cosine Similarity)**(의미)를 사용하여 유사한 기사를 그룹화합니다. 각 클러스터에서 가장 점수가 높은 하나의 기사만 남겨 중복(예: 동일한 보도 자료를 50개 언론사에서 보도)을 제거합니다.

#### **2단계: 큐레이션 (Curation) - 인간 참여형 (Human-in-the-Loop)**
**목표**: AI를 인간의 의도에 맞게 조정하고 훈련 데이터를 생성합니다.
1.  **UI 실행**: 관리자 대시보드(Admin Dashboard)에 상위 약 50개의 후보를 표시합니다.
2.  **선별**: 인간 전문가는 AI가 놓칠 수 있는 직관을 사용하여 중요한 항목에 "선택(Select)"을 표시합니다.
3.  **피드백 루프**: "AI 제안"과 "인간 선택" 간의 차이가 저장됩니다. 이 데이터는 1단계의 **IRL 모델**이 시간이 지남에 따라 사용자의 구체적인 취향을 학습하는 데 사용됩니다 (능동적 학습, Active Learning).

#### **3단계: 분석가 (Analyst) - 인지 추론**
**목표**: 깊이 있고 인과관계가 명확한 전략적 통찰력을 생성합니다.
1.  **컨텍스트 검색 (역사가, The Historian)**:
    *   뉴스에서 개체(Entity)를 추출합니다 (예: "BYD", "헝가리").
    *   **Graph-RAG**: Neo4j에 **2-Hop 연결**을 쿼리합니다 (`Match (e)-[*1..2]-(historical_event)`).
    *   *결과*: 숨겨진 연결 고리를 찾습니다 (예: "BYD는 2년 전 헝가리에 공장을지었다" -> "왜 헝가리가 BYD에 대한 EU 관세를 지지하는지 설명").
2.  **다중 에이전트 토론 프로토콜 (The Analyst)**:
    *   **계획자 (Planner)**: "이 건은 공급망 붕괴 기회로 분석해야 합니다."라며 분석 전략을 낙관적으로 수립합니다.
    *   **시뮬레이터 (Simulator)**: "반사실적(Counterfactual) 실행: 만약 이 관세 조치가 없었다면 어떻게 되었을까?"
    *   **악마의 변호인 (Devil's Advocate)**: "비평: 계획자는 4조의 허점을 무시하고 있습니다. 영향이 과대평가되었습니다."
    *   **종합자 (Synthesizer)**: "인정합니다. 4조의 허점을 고려하여 계획을 수정하겠습니다."
    *   **작가 (Writer)**: 종합된 진실을 바탕으로 최종 논평(Commentary)을 작성합니다.

#### **4단계: 내보내기 (Export) - 시각화**
**목표**: 통찰력을 전달 가능하고 시각적으로 유지합니다.
1.  **지표 파싱**: 에이전트의 텍스트 출력에서 `신뢰도 점수(Confidence Score)`와 `시간 지평(Time Horizon)` 같은 정형 데이터를 정규식(Regex)으로 추출합니다.
2.  **Mermaid 생성**: LLM이 분석 내용을 읽고 원인과 결과의 연쇄를 시각적으로 매핑하는 **시퀀스 다이어그램(Sequence Diagram)** 코드를 생성합니다.
3.  **보고서 조립**: 텍스트, 메타데이터 배지, 렌더링된 다이어그램을 구조화된 Markdown/HTML 보고서로 컴파일합니다.

---

## 🛠 기술 스택 (Technology Stack)

| 컴포넌트 | 기술 | 선정 이유 |
| :--- | :--- | :--- |
| **LLM 코어** | `Google Gemini 3 flash` | 거대한 컨텍스트 창 (1M 토큰) + 토론 루프를 위한 빠른 속도. |
| **임베딩** | `SBERT (mpnet-base-v2)` | 필터링을 위한 동급 최고의 오픈 소스 의미 검색 모델. |
| **그래프 DB** | `Neo4j` | 복잡한 공급망 및 관계 모델링을 위해 필수 (Graph-RAG). |
| **백엔드** | `FastAPI + Uvicorn` | 대시보드를 위한 실시간 로그 스트리밍 및 프로세스 관리. |
| **프론트엔드** | `HTML/JS (Vanilla)` | 가볍고 반응이 빠른 관리자 대시보드. |
| **파이프라인** | `Python 3.12` | 명확한 순차 실행을 위해 01-04번으로 번호가 매겨진 스크립트. |

---

## 🚀 시작하기 (Getting Started)

### 1. 필수 조건
*   **Python 3.12+**
*   **Neo4j 데이터베이스**:
    *   Docker로 실행: `docker run -p 7474:7474 -p 7687:7687 -e NEO4J_AUTH=none neo4j`
    *   *확인*: `http://localhost:7474`를 열어 실행 중인지 확인합니다.
*   **API 키**:
    *   `GOOGLE_API_KEY`: 분석가 (Gemini) 사용에 필수.
    *   `OPENAI_API_KEY`: 선택 사항 (대체용).

### 2. 설치
```bash
# 리포지토리 복제
git clone https://github.com/YourRepo/Autowein.git
cd Autowein

# 가상 환경 생성 및 활성화
python3 -m venv venv
source venv/bin/activate

# 의존성 설치
pip install -r requirements.txt
python -m spacy download en_core_web_sm
```

### 3. 구성 (Configuration)
`config/mobility.yaml`을 편집하여 사용자 정의합니다:
*   **Keywords**: 새로운 주제 추가 (예: "전고체 배터리").
*   **Sources**: 신뢰할 수 있는 뉴스 도메인 추가/제거.

---

## 🖥️ 시스템 실행 (관리자 대시보드)

Autowein을 실행하는 권장 방법은 웹 대시보드를 사용하는 것입니다. 파이프라인 진행 상황을 시각화하고 결과를 즉시 확인할 수 있습니다.

### 1단계: 대시보드 서버 실행
```bash
./venv/bin/python -m uvicorn web_app.api.main:app --host 0.0.0.0 --port 8000
```

### 2단계: 액세스 및 제어
브라우저에서 **http://localhost:8000**을 엽니다.

1.  **뷰 (사이드바)**:
    *   **실시간 로그 (Live Logs)**: 파이프라인의 실시간 터미널 출력을 봅니다.
    *   **결과 (Results)**: 과거 인텔리전스 보고서 및 분석된 항목을 탐색합니다.
2.  **파이프라인 제어**:
    *   **전체 시퀀스 실행 (Run Full Sequence)**: 1단계 → 4단계를 자동으로 실행합니다.
    *   **개별 단계 실행**: 특정 버튼(01 Selection, 02 Curation 등)을 클릭하여 단계를 수동으로 실행합니다.

---

## 📁 디렉토리 구조
```bash
Autowein/
├── config/             # YAML 구성 (가중치 키워드, 소스, 프롬프트)
├── data/               # 일일 아티팩트 (JSON, 보고서)
│   └── daily/
│       └── YYYY-MM-DD/ # 특정 날짜의 모든 입력/출력
├── docker/             # 배포 스크립트 (Dockerfile, deploy.sh)
├── scripts/
│   ├── pipeline/       # 4단계 파이프라인 (01_selection -> 04_export)
│   └── tools/          # 유틸리티 (train_irl.py, 디버그 스크립트)
├── src/
│   ├── gatekeeper/     # SBERT 모델, 스크래퍼, IRL 엔진
│   ├── historian/      # Neo4j 그래프 관리자 및 Cypher 쿼리
│   ├── analyst/        # LLM 에이전트 (악마, 계획자, 작가)
│   ├── core/           # 데이터 스키마 (Pydantic 모델)
│   └── dashboard/      # (레거시) Streamlit 앱
├── web_app/            # 새로운 관리자 대시보드
│   ├── api/            # FastAPI 백엔드
│   └── ui/             # HTML/JS 프론트엔드
└── tests/              # 단위 테스트
```

---

## ❓ 문제 해결 (Troubleshooting)

**Q: `CUDA error: no kernel image is available` (GTX 10xx 시리즈)**
*   **해결**: 코드는 `src/gatekeeper/models.py`에서 구형 GPU(Compute Capability < 7.0)를 자동으로 감지하고 충돌을 방지하기 위해 **CPU 모드**를 강제합니다. 별도의 조치가 필요하지 않습니다.

**Q: Neo4j 연결 거부됨 (Connection Refused)**
*   **해결**: Docker 컨테이너가 실행 중인지(`docker ps`), 7687 포트가 노출되어 있는지 확인하세요.

**Q: 점수 모델의 "훈련된 가중치"는 어디에 있습니까?**
*   **답변**: 시스템은 "시뮬레이션 모드"(초기화된 가중치)에서 시작합니다. 매일 뉴스를 큐레이션(2단계)하면 데이터가 축척됩니다. 주기적으로 `python scripts/tools/train_irl.py`를 실행하여 사용자의 선호도에 맞춰 모델을 훈련시키세요.
